name: Workflow Run
on:
  workflow_run:
    workflows: [Chama workflow compartilhado]
    types:
      - completed
   

jobs:
  fala-o-nome-do-curso:
    runs-on: ubuntu-latest
    steps:
      - name: Informa o nome do curso
        run: echo "O workflow compartilhado 'Chama Workflow Compartilhado' foi executado"

