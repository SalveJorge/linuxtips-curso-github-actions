name: criando nossa trigger schedule
on:
  schedule:
    - cron: '37 * * * 1'


jobs:
  execute-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Informa horario
        run: echo " schedule executado com sucesso, agora sao $(date) UTC"
